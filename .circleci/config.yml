version: 2.1

jobs:
  build:
    docker:
      - image: puchuu/test-compression_i686-gentoo-linux-musl:latest
    working_directory: /mnt/data
    steps:
      - checkout
      - run: /mnt/data/scripts/test-images/ci_test.sh

workflows:
  build-deploy:
    jobs:
      - build:
          filters:
            branches:
              only: master
