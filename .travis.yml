language: ruby
rvm:
  - 2.6

os:
  - linux
  - osx

arch:
  - amd64
  - arm64

dist: bionic

addons:
  apt:
    packages:
      - libzstd-dev
    update: true
  homebrew:
    packages:
      - zstd
    update: true

before_install:
  - gem install bundler

script:
  - scripts/ci_test.sh
